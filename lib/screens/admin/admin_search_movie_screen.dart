import 'package:flutter/material.dart';

class AdminSearchMovieScreen extends StatelessWidget {
  const AdminSearchMovieScreen({super.key});

  static const String id = "/admin-search-movie-screen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: []),
    );
  }
}
